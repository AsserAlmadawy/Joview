// vite.config.js
import { defineConfig } from "vite";

export default defineConfig({
  // You can add more configurations here if needed,
  // such as `base` for deployment paths or `build` options.
});